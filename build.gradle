group 'com.ss'
version '2.1'

apply plugin: 'java'

repositories {
    mavenCentral()
    maven { url 'https://jitpack.io' }
}

defaultTasks 'copyJarToRelease'

task copyJarToRelease {
    copy {
        from './lib/tonegod.emitter-2.1.jar'
        into "./release/"
    }
}

