group 'com.ss'
version '2.1'

apply plugin: 'java'

repositories {
    mavenCentral()
    maven { url 'https://jitpack.io' }
}

dependencies {
    //compile 'com.github.javasabr:tonegodemitter:temp-SNAPSHOT'
}

copy {
    from(zipTree("./artefacts/tonegod.emitter-2.1.jar"))
    into("./build/classes/")
}

jar {
    baseName = 'tonegod.emitter'
    version =  '2.1'
}

defaultTasks 'clean', 'build'
